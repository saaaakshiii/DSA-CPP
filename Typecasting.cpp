#include <iostream>

using namespace std;

int main()
{
    // int x = 1;
    // char ch = (char)x;

    // cout << (ch + 7);

    // char ch='*';
    // int y= (int) ch;
    // cout<<(ch+3);

    char ch = 'A';

    for (int i = 0; i < 9; i++)
    {
        cout << (char)('A' + i);
    }

    return 0;
}